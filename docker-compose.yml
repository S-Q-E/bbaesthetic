services:
  bot:
    build:
      context: .
    container_name: cosmetology-leads-bot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
      - bot_tmp:/app/tmp

volumes:
  bot_data:
  bot_tmp:
